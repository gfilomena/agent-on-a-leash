# Template of .env without secrets (safe to commit). Copy it to .env and fill in the keys.

# Viseca hackathon API (the team key handed out by Viseca)
LEASH_BASE_URL=https://saw26api.ashyground-364e1d07.switzerlandnorth.azurecontainerapps.io
TEAM_API_KEY=

# AI model provider (OpenAI)
OPENAI_API_KEY=
# Small, fast model (Viseca's preference). Leave empty: Claude Code will propose one.
OPENAI_MODEL=
